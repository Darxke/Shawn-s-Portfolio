# run_5
import streamlit as st
def runApp():
    st.header(":five: Running the App")
    st.write("Run your streamlit app")
    st.write("Make sure you are in the right directory using `cd`")
    st.code("streamlit run Your_App.py")
    st.write("Make sure you are in the write directory in your terminal or command prompt")
